\paragraph{Parallel theorem-candidate comparison (v10.3).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=1.253e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=7.881e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=7.881e-04$ and $F(1)_\infty=-1.575e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=4.945e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=3.108e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=3.108e-04$ and $F(1)_\infty=-6.214e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=2.749e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=1.727e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=1.727e-04$ and $F(1)_\infty=-3.454e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=1.037e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=6.512e-05$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=6.512e-05$ and $F(1)_\infty=-1.303e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=4.021e-06$, $(a_{\mathrm{nc}}/r_c)_\infty-1=2.525e-05$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=2.525e-05$ and $F(1)_\infty=-5.053e-05$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
