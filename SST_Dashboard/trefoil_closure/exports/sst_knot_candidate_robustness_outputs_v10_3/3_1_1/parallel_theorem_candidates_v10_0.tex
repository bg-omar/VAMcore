\paragraph{Parallel theorem-candidate comparison (v10.3).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=-1.369e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-8.646e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-8.646e-04$ and $F(1)_\infty=1.721e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=-5.421e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-3.455e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-3.455e-04$ and $F(1)_\infty=6.812e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=-9.004e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-5.708e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-5.708e-04$ and $F(1)_\infty=1.131e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=-7.700e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-4.842e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-4.842e-03$ and $F(1)_\infty=9.676e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=-5.613e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-3.524e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-3.524e-03$ and $F(1)_\infty=7.053e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
