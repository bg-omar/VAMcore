\paragraph{v10.2 asymptotic extrapolation.}
Using the converged high-resolution tail, we fit $y(N_{\mathrm{int}})=y_{\infty}+cN_{\mathrm{int}}^{-\alpha}$, obtaining $A_\infty=0.0792712608$ with $A_\infty-1/(4\pi)=-3.062e-04$, and $(a_{\mathrm{nc}}/r_c)_\infty=0.9980681813$ with $(a_{\mathrm{nc}}/r_c)_\infty-1=-1.932e-03$. The observed high-resolution tail is quasi-monotone toward the target for $A_K$ (yes) and for $a_{\mathrm{nc}}/r_c$ (yes). For the shift-free postcheck at $\lambda_K=0$, $(a^*/r_c)_\infty=0.9980681813$ with deviation -1.932e-03, and $F(1)_\infty=3.848e-03$. The asymptotic tail uses root-choice labels \texttt{targeted_x_nc:3}. Tail checks: $a^*/r_c$ (yes), $F(1)$ (yes). A theorem-style closure statement is recorded only when both the extrapolated limit and the
observed high-resolution tail support the same conclusion.
