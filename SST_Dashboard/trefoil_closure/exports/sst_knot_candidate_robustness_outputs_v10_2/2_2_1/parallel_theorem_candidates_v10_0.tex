\paragraph{Parallel theorem-candidate comparison (v10.2).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=1.410e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=8.859e-04$, no-contact tail checks: $A_K$ (yes), $a_{\mathrm{nc}}/r_c$ (yes). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=8.859e-04$ and $F(1)_\infty=-1.772e-03$, with tail checks $a^*/r_c$ (yes) and $F(1)$ (yes). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=5.550e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=3.486e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=3.486e-04$ and $F(1)_\infty=-6.974e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=-3.450e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-2.168e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-2.168e-04$ and $F(1)_\infty=4.336e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=8.853e-06$, $(a_{\mathrm{nc}}/r_c)_\infty-1=5.562e-05$, no-contact tail checks: $A_K$ (yes), $a_{\mathrm{nc}}/r_c$ (yes). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=5.562e-05$ and $F(1)_\infty=-1.112e-04$, with tail checks $a^*/r_c$ (yes) and $F(1)$ (yes). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=4.599e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=2.890e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=2.890e-04$ and $F(1)_\infty=-5.779e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
