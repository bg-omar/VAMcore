\paragraph{Parallel theorem-candidate comparison (v10.2).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=1.414e-02$, $(a_{\mathrm{nc}}/r_c)_\infty-1=8.919e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=8.919e-02$ and $F(1)_\infty=-1.777e-01$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=1.472e-02$, $(a_{\mathrm{nc}}/r_c)_\infty-1=9.197e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=9.197e-02$ and $F(1)_\infty=-1.849e-01$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=8.262e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=5.198e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=5.198e-02$ and $F(1)_\infty=-1.038e-01$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=6.847e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=4.316e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=4.316e-02$ and $F(1)_\infty=-8.604e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=5.903e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=3.719e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=3.719e-02$ and $F(1)_\infty=-7.418e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
