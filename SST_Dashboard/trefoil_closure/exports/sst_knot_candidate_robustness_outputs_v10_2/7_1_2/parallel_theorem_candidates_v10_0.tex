\paragraph{Parallel theorem-candidate comparison (v10.2).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=6.216e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=3.899e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=3.899e-03$ and $F(1)_\infty=-7.811e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=6.892e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=4.318e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=4.318e-02$ and $F(1)_\infty=-8.660e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=6.523e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=4.087e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=4.087e-02$ and $F(1)_\infty=-8.197e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=4.262e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=2.676e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=2.676e-02$ and $F(1)_\infty=-5.356e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=7.292e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=4.568e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=4.568e-03$ and $F(1)_\infty=-9.164e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
