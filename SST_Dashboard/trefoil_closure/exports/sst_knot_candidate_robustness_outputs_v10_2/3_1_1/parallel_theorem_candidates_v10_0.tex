\paragraph{Parallel theorem-candidate comparison (v10.2).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=1.279e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=8.039e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=8.039e-04$ and $F(1)_\infty=-1.607e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=-8.097e-06$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-5.110e-05$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-5.110e-05$ and $F(1)_\infty=1.017e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=8.257e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=5.185e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=5.185e-04$ and $F(1)_\infty=-1.038e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=-1.244e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-7.821e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-7.821e-04$ and $F(1)_\infty=1.564e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=-1.015e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-6.379e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-6.379e-03$ and $F(1)_\infty=1.276e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
