\paragraph{v9.2 post-sweep shift-free barrier check.}
After the full legacy sweep, the preferred-fit rows were re-evaluated with the shift-free barrier contact model \texttt{barrier\_flat\_at\_rc}, without altering any legacy outputs. For the two highest $N_{\mathrm{int}}$ rows at $\lambda_K=0$, one finds $F(1) = 5.20519396e-04 \pm 1.04631232e-03$ and $a^*/r_c = 0.99973957 \pm 0.00052329$. Thus the shift-free postcheck isolates the remaining closure error to the residual deviation of $A_K$ from $1/(4\pi)$, rather than to a structurally shifting contact term.
