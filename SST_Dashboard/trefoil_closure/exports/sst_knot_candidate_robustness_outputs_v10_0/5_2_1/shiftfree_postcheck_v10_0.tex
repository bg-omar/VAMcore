\paragraph{v10.0 post-sweep shift-free barrier check.}
After the full legacy sweep, the preferred-fit rows were re-evaluated with the shift-free barrier contact model \texttt{barrier\_flat\_at\_rc}, without altering any legacy outputs. For the two highest $N_{\mathrm{int}}$ rows at $\lambda_K=0$, one finds $F(1) = nan \pm nan$ and $a^*/r_c = nan \pm nan$. Thus the shift-free postcheck isolates the remaining closure error to the residual deviation of $A_K$ from $1/(4\pi)$, rather than to a structurally shifting contact term.
