\paragraph{Parallel theorem-candidate comparison (v10.0).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=2.631e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=1.652e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=1.652e-03$ and $F(1)_\infty=-3.306e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=1.269e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=7.974e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=7.974e-04$ and $F(1)_\infty=-1.595e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=6.914e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=4.345e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=4.345e-03$ and $F(1)_\infty=-8.688e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=5.624e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=3.536e-04$, no-contact tail checks: $A_K$ (yes), $a_{\mathrm{nc}}/r_c$ (yes). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=3.536e-04$ and $F(1)_\infty=-7.068e-04$, with tail checks $a^*/r_c$ (yes) and $F(1)$ (yes). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=-1.213e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-7.609e-05$, no-contact tail checks: $A_K$ (yes), $a_{\mathrm{nc}}/r_c$ (yes). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-7.609e-05$ and $F(1)_\infty=1.524e-04$, with tail checks $a^*/r_c$ (yes) and $F(1)$ (yes). This report is diagnostic only: it does not alter the practical best-estimate selection.
