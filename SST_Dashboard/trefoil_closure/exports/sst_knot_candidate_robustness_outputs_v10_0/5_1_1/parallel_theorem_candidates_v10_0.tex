\paragraph{Parallel theorem-candidate comparison (v10.0).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=3.003e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=1.928e-03$, no-contact tail checks: $A_K$ (yes), $a_{\mathrm{nc}}/r_c$ (yes). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=1.928e-03$ and $F(1)_\infty=-3.773e-03$, with tail checks $a^*/r_c$ (yes) and $F(1)$ (yes). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=2.668e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=1.681e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=1.681e-02$ and $F(1)_\infty=-3.353e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=2.180e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=1.373e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=1.373e-02$ and $F(1)_\infty=-2.740e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=1.336e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=8.408e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=8.408e-03$ and $F(1)_\infty=-1.678e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=1.610e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=1.014e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=1.014e-02$ and $F(1)_\infty=-2.023e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
