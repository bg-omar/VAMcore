\paragraph{v10.3_master asymptotic extrapolation.}
Using the converged high-resolution tail, we fit $y(N_{\mathrm{int}})=y_{\infty}+cN_{\mathrm{int}}^{-\alpha}$, obtaining $A_\infty=0.0787865632$ with $A_\infty-1/(4\pi)=-7.909e-04$, and $(a_{\mathrm{nc}}/r_c)_\infty=0.9950358110$ with $(a_{\mathrm{nc}}/r_c)_\infty-1=-4.964e-03$. The observed high-resolution tail is quasi-monotone toward the target for $A_K$ (no) and for $a_{\mathrm{nc}}/r_c$ (no). For the shift-free postcheck at $\lambda_K=0$, $(a^*/r_c)_\infty=0.9950358110$ with deviation -4.964e-03, and $F(1)_\infty=9.939e-03$. The asymptotic tail uses root-choice labels \texttt{targeted_x_nc:10}. Tail checks: $a^*/r_c$ (no), $F(1)$ (no). A theorem-style closure statement is recorded only when both the extrapolated limit and the
observed high-resolution tail support the same conclusion.
