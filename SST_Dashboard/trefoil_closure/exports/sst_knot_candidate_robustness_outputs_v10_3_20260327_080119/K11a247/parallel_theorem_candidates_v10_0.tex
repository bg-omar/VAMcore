\paragraph{Parallel theorem-candidate comparison (v10.3_master).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=4.129e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=2.574e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=2.574e-03$ and $F(1)_\infty=-5.189e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=7.374e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=4.451e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=4.451e-04$ and $F(1)_\infty=-9.266e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=4.470e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=2.810e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=2.810e-03$ and $F(1)_\infty=-5.617e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=1.221e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=7.594e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=7.594e-04$ and $F(1)_\infty=-1.534e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=1.434e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=9.016e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=9.016e-04$ and $F(1)_\infty=-1.802e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
