\paragraph{Parallel theorem-candidate comparison (v10.3_master).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=-1.308e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-8.548e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-8.548e-04$ and $F(1)_\infty=1.643e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=-5.951e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-3.850e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-3.850e-04$ and $F(1)_\infty=7.478e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=9.097e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=5.445e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=5.445e-04$ and $F(1)_\infty=-1.143e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=7.789e-05$, $(a_{\mathrm{nc}}/r_c)_\infty-1=4.854e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=4.854e-04$ and $F(1)_\infty=-9.788e-04$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=2.542e-06$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-4.101e-06$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-4.101e-06$ and $F(1)_\infty=-3.194e-05$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
