\paragraph{Parallel theorem-candidate comparison (v10.2).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=-6.247e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-3.927e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-3.927e-03$ and $F(1)_\infty=7.851e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=-3.561e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-2.242e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-2.242e-03$ and $F(1)_\infty=4.475e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=-1.005e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-6.355e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-6.355e-03$ and $F(1)_\infty=1.263e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=-8.551e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-5.377e-03$, no-contact tail checks: $A_K$ (yes), $a_{\mathrm{nc}}/r_c$ (yes). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-5.377e-03$ and $F(1)_\infty=1.075e-02$, with tail checks $a^*/r_c$ (yes) and $F(1)$ (yes). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=-5.850e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-3.677e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-3.677e-03$ and $F(1)_\infty=7.351e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
