\paragraph{Parallel theorem-candidate comparison (v10.2).}
We evaluated multiple plateau-based extraction rules in parallel, keeping the numerical sweep, root solver, and shift-free postcheck fixed. For \texttt{plateau_0.04}, $A_\infty-1/(4\pi)=8.067e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=5.062e-03$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=5.062e-03$ and $F(1)_\infty=-1.014e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.06}, $A_\infty-1/(4\pi)=3.896e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=2.437e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=2.437e-02$ and $F(1)_\infty=-4.896e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.08}, $A_\infty-1/(4\pi)=-2.146e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-1.336e-02$, no-contact tail checks: $A_K$ (yes), $a_{\mathrm{nc}}/r_c$ (yes). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-1.336e-02$ and $F(1)_\infty=2.696e-02$, with tail checks $a^*/r_c$ (yes) and $F(1)$ (yes). For \texttt{plateau_0.10}, $A_\infty-1/(4\pi)=1.308e-04$, $(a_{\mathrm{nc}}/r_c)_\infty-1=8.220e-04$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=8.220e-04$ and $F(1)_\infty=-1.644e-03$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). For \texttt{plateau_0.12}, $A_\infty-1/(4\pi)=-2.152e-03$, $(a_{\mathrm{nc}}/r_c)_\infty-1=-1.350e-02$, no-contact tail checks: $A_K$ (no), $a_{\mathrm{nc}}/r_c$ (no). Shift-free extrapolation gives $(a^*/r_c)_\infty-1=-1.350e-02$ and $F(1)_\infty=2.704e-02$, with tail checks $a^*/r_c$ (no) and $F(1)$ (no). This report is diagnostic only: it does not alter the practical best-estimate selection.
